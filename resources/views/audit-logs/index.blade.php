@extends('layouts.absensi')
@section('title','Audit Trail - PT. INTI')
@section('content')
<div class="flex flex-col gap-space-lg"><div><span class="text-[11px] font-semibold text-on-surface-variant uppercase tracking-wider">GOVERNANCE</span><h1 class="font-headline-lg text-primary mt-1">Audit Trail</h1><p class="text-on-surface-variant mt-1">Riwayat aksi penting pada presensi dan workflow lembur.</p></div><form class="bg-surface-container-lowest rounded-2xl p-space-lg shadow-sm flex flex-wrap gap-3 items-end"><div><label class="text-xs">Action</label><input name="action" value="{{ $filters['action'] ?? '' }}" placeholder="CLOCK_OUT / HR_VERIFIED" class="block rounded-lg border-outline-variant mt-1"></div><div><label class="text-xs">Dari</label><input type="date" name="from" value="{{ $filters['from'] ?? '' }}" class="block rounded-lg border-outline-variant mt-1"></div><div><label class="text-xs">Sampai</label><input type="date" name="until" value="{{ $filters['until'] ?? '' }}" class="block rounded-lg border-outline-variant mt-1"></div><button class="rounded-lg bg-primary-container text-white px-5 py-2 font-semibold">Filter</button></form><div class="bg-surface-container-lowest rounded-2xl shadow-sm overflow-hidden"><div class="overflow-x-auto"><table class="w-full min-w-[1000px] text-left"><thead class="bg-surface-container-low text-xs uppercase text-on-surface-variant"><tr><th class="p-3">Waktu</th><th class="p-3">User</th><th class="p-3">Action</th><th class="p-3">Subject</th><th class="p-3">Catatan</th><th class="p-3">IP</th></tr></thead><tbody class="divide-y divide-surface-container-low">@forelse($logs as $log)<tr><td class="p-3 whitespace-nowrap">{{ $log->created_at?->format('d/m/Y H:i:s') }}</td><td class="p-3">{{ $log->user?->name ?? 'System' }}</td><td class="p-3"><span class="px-2 py-1 rounded-full bg-secondary/10 text-secondary text-xs font-semibold">{{ $log->action }}</span></td><td class="p-3 text-xs">{{ class_basename($log->subject_type ?? '-') }} #{{ $log->subject_id ?? '-' }}</td><td class="p-3 max-w-md">{{ $log->notes ?? '-' }}</td><td class="p-3 font-mono text-xs">{{ $log->ip_address ?? '-' }}</td></tr>@empty<tr><td colspan="6" class="p-8 text-center text-on-surface-variant">Belum ada audit log.</td></tr>@endforelse</tbody></table></div><div class="p-4">{{ $logs->links() }}</div></div></div>
@endsection
